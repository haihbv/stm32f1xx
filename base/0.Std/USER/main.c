#include "gpio.h"                  // Device header


int main()
	
{
	//config_led();
	//GPIOx_Init(GPIOC, GPIO_Pin_13, GPIO_Mode_Out_PP, GPIO_Speed_50MHz);
	PC13
	while(1)
	{
		blink();
	}
}
